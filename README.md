# findDistinctOptions-using-Python

A small Python utility to determine distinct combinations/options from given inputs. Built to assist in scenarios such as selection filtering, enumeration of unique options and deduplication logic.

## 📌 Features

- Reads input (e.g., list of items or options) and computes distinct combinations or options.
- Uses Python for quick prototyping and can be extended for larger datasets or more complex logic.
- Simple and clean script suitable for learning, reuse, or as snippet in larger projects.

## 🧰 Tech Stack

- Python (single file: `findDistinctOptions.py`)
- No external dependencies (pure Python)  
